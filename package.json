{
  "name": "elewacja-pro",
  "version": "1.0.0",
  "private": true,
  "description": "ElewacjaPro — Kalkulator materialow elewacyjnych ETICS",
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/core": "^6.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0",
    "esbuild": "0.20.2"
  },
  "scripts": {
    "prepare-web": "bash android-setup.sh",
    "sync": "npx cap sync android",
    "open-android": "npx cap open android",
    "build-android": "bash android-setup.sh && npx cap sync android && npx cap open android"
  }
}
